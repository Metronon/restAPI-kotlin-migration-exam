pluginManagement {
    plugins {
        kotlin("jvm") version "1.9.25" // Use a valid version
    }

    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "kotlin-migration"

